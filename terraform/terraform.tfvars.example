# Copy to terraform.tfvars and adjust if needed.
# terraform.tfvars is gitignored — never commit it.

aws_region   = "us-east-1"
project_name = "vpc-guestbook"

# Set a real, strong password here in your own terraform.tfvars —
# never commit an actual password. This file is just the template.
db_username = "guestbook_admin"
db_password = "CHANGE-ME-to-a-real-strong-password"
